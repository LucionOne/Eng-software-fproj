
# the project needs to have three main softwares

## The manager
- my main challenge
- get data from mock data api
- store the data in a sqlite database
- be an api for the dashboard

- should i use Python, C# or Java?
- Python, why? makes life easy for simple projects and i don't plan to scale any of this

- shall make use of `sqlite3` and `FastAPI` libs
- will need to make use of threads

## The dashboard
- has two possibilities

- can be made with html file
- can use javascript to request data from the manager by itself
- can be one file
- OR
- make manager an server
- has a html as a gui

## The mock data
- needs to feed the manager with enough data
- if extra time make believable data
- python should enough

- json server?